# Références
- `engine-prototype.js` : moteur pur validé en ateliers (règles R01–R16, actions, messages de refus). **Source de la logique** à porter en TypeScript. Écarts arbitrés : voir la fin de `docs/02-regles-metier.md` (perte à J+2 18h, dates réelles, données de `data/seed.json`).
- `prototype-full.html` : ancien prototype de validation des règles (s'ouvre dans un navigateur). À utiliser pour **comprendre les comportements**, jamais pour le style : l'identité visuelle à suivre est celle du Figma.
- `charte/` : logos MyDigitalSchool.
